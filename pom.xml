<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>org.lareferencia</groupId>
    <artifactId>lareferencia-lrharvester-admin-web</artifactId>
    <version>5.0.0-rc</version>
    <packaging>pom</packaging>

    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <node.version>v22.14.0</node.version>
        <npm.version>10.9.2</npm.version>
        <harvester.static.dir>${project.basedir}/../lareferencia-lrharvester-app/static</harvester.static.dir>
    </properties>

    <build>
        <plugins>
            <!-- Node is pinned here so local and CI builds produce the same frontend. -->
            <plugin>
                <groupId>com.github.eirslett</groupId>
                <artifactId>frontend-maven-plugin</artifactId>
                <version>1.15.1</version>
                <executions>
                    <execution>
                        <id>install-node-and-npm</id>
                        <goals>
                            <goal>install-node-and-npm</goal>
                        </goals>
                        <configuration>
                            <nodeVersion>${node.version}</nodeVersion>
                            <npmVersion>${npm.version}</npmVersion>
                        </configuration>
                    </execution>
                    <execution>
                        <id>npm-ci</id>
                        <goals>
                            <goal>npm</goal>
                        </goals>
                        <configuration>
                            <arguments>ci --no-audit --no-fund</arguments>
                        </configuration>
                    </execution>
                    <execution>
                        <id>build-frontend</id>
                        <phase>prepare-package</phase>
                        <goals>
                            <goal>npm</goal>
                        </goals>
                        <configuration>
                            <arguments>run build</arguments>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <!-- The harvester reads this external directory directly; target is never served. -->
            <plugin>
                <artifactId>maven-antrun-plugin</artifactId>
                <version>3.1.0</version>
                <executions>
                    <execution>
                        <id>remove-macos-metadata-before-npm</id>
                        <phase>initialize</phase>
                        <configuration>
                            <target>
                                <delete dir="${project.basedir}/node_modules" includeemptydirs="true" quiet="true"/>
                            </target>
                        </configuration>
                        <goals><goal>run</goal></goals>
                    </execution>
                    <execution>
                        <id>publish-to-harvester-static</id>
                        <phase>package</phase>
                        <configuration>
                            <target>
                                <mkdir dir="${harvester.static.dir}"/>
                                <delete includeemptydirs="true" failonerror="true">
                                    <fileset dir="${harvester.static.dir}"/>
                                </delete>
                                <copy todir="${harvester.static.dir}" overwrite="true">
                                    <fileset dir="${project.basedir}/dist"/>
                                </copy>
                            </target>
                        </configuration>
                        <goals>
                            <goal>run</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>
